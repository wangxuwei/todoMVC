@import "../pcss/utils.pcss";

.TodoList{
	flex: 1;
	display: flex;
	flex-direction: column;
	.add-con{
		margin-top: 32px;
		input{
			width: 300px;
		}
		.btn-add{
			margin-top: 16px;
		}
	}

	.items-con{
		flex: 1;
		margin: 24px 0;
		border: 1px solid #ccc;
		border-radius: 4px;
		.item{
			border-top: 1px solid #ccc;
			&:first-child{
				border-top: none;
			}
			@mixin clearfix;
			.name{
				cursor: pointer;
				margin-left: 16px;
				display:block;
				line-height: 32px;
				margin-right: 160px;
			}
			.btn{
				float:right;
				height: 24px;
				line-height: 22px;
				padding: 0px 4px;
				margin: 4px 16px;
				text-align:center;
			}
		}
	}
}